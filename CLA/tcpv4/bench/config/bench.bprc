# bprc for the TCPCLv4 CLA benchmark.  PROTO, DUCT and CLICMD are
# substituted at runtime, one set per mode being measured, so that every
# mode is driven through exactly the same node configuration.
1
# ION acquires a bundle into the SDR heap only while it fits
# maxAcqInHeap, and spools it through a file otherwise - one
# open/write/close/unlink per bundle, which costs far more than the
# convergence layer does.  The default is 560 bytes, so without this
# line essentially every bundle is written to disk on reception.
m heapmax 262144
a scheme ipn 'ipnfw' 'ipnadminep'
a endpoint ipn:1.1 q
a endpoint ipn:1.2 q
m payloadcrc 0
a protocol PROTO 1400 100
a induct  PROTO DUCT 'CLICMD'
a outduct PROTO DUCT ''
r 'ipnadmin config/bench.ipnrc'
s
